# wifi-sentinel configuration
# Edit this file to customise behaviour. Changes take effect on the next scan.

# Send desktop notifications (true/false)
NOTIFY=true

# Automatically add clean networks to trusted list when running via dispatcher.
# When running manually you will always be prompted regardless of this setting.
AUTO_TRUST=true
